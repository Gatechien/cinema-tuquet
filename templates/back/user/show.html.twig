{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="container-xl bg-light p-5 border-top">
    <h1>Utilisateur</h1>

    <a type="button" href="{{ path('app_back_user_index') }}"><span class="badge bg-warning text-dark fs-6 mx-2 my-2"> Retour à la liste </span></a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" href="{{ path('app_back_user_edit', {'id': user.id}) }}"><span class="badge bg-warning text-dark fs-6 ms-2"> Editer </span></a>

    {{ include('back/user/_delete_form.html.twig') }}
</div>
{% endblock %}
