{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
<div class="container-xl bg-light p-5 border-top">
    <h1>Genre</h1>

    <a type="button" href="{{ path('app_back_genre_index') }}"><span class="badge bg-warning text-dark fs-6 mx-2 my-2"> Retour à la liste </span></a>

    <table class="table">
        <tbody>
            <tr>
                <th>GenreId</th>
                <td>{{ genre.genreId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" href="{{ path('app_back_genre_edit', {'genreId': genre.genreId}) }}"><span class="badge bg-warning text-dark fs-6 ms-2"> Editer </span></a>

    {{ include('back/genre/_delete_form.html.twig') }}
</div>
{% endblock %}
